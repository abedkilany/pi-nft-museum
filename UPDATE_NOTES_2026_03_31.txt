Updated from the latest uploaded version only.

Included in this update:
- New mint status flow: UNMINTED / LAZY_MINTED / MINTED
- Lazy Mint now stores LAZY_MINTED instead of MINTED
- Gallery/detail labels now show Not minted yet / Off-chain / On-chain
- New simple Manage popup in My Artworks
- Manage popup edits only: base price, discount, final price (calculated), listing type, visibility
- New protected API route: /api/account/artworks/manage
- New Prisma migration: 20260331_lazy_mint_status

Important after replacing the project files:
1) npx prisma migrate deploy
2) npx prisma generate

If your database already contains artworks previously lazy-minted as MINTED, the new migration converts them to LAZY_MINTED.
