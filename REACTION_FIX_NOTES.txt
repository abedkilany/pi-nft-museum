Applied reaction fix on 2026-03-28.

Changed files:
- components/reactions/ReactionButtons.tsx
- app/gallery/page.tsx
- app/premium/page.tsx
- app/api/reactions/toggle/route.ts

What was fixed:
1) Client buttons now update local counts and local reaction state immediately after a successful toggle.
2) Gallery and Premium pages now load the signed-in user's existing reaction and pass it to the buttons.
3) Reaction toggle API now revalidates /gallery, /premium, and /artwork/[id] after updates so cached pages refresh correctly.

Note:
- The repository still has many pre-existing TypeScript issues unrelated to this fix.
- I did not fix those unrelated project-wide errors.
