Lazy mint update summary
- Added off-chain lazy mint snapshot, ownership, and ownership history Prisma models.
- Added migration: prisma/migrations/20260328_lazy_mint_snapshot
- Lazy Mint now creates a fixed snapshot and publishes artwork to gallery.
- On-chain Mint button is shown as disabled placeholder.
- Ratings remain review-only.
- Comments are now allowed only after publication.
- Editing is now locked once artwork leaves DRAFT.

After extracting locally:
1) npm install (if needed)
2) npx prisma generate
3) npx prisma migrate dev
4) run the app normally
